diff --git a/dist/index-CR4-ffm_.d.ts b/dist/index-CR4-ffm_.d.ts
index 7f7590d2ded8fd0f25f97937fcdc05fe7f5ef57f..232d8fcefda233ae2697f45f853f2a2acc61f7b6 100644
--- a/dist/index-CR4-ffm_.d.ts
+++ b/dist/index-CR4-ffm_.d.ts
@@ -817,6 +817,16 @@ interface TurnConfig {
   experimental_transform?: Parameters<
     typeof streamText
   >[0]["experimental_transform"];
+  /**
+   * Optional tool-call repair callback. Forwarded to streamText as
+   * `experimental_repairToolCall`. Invoked when a tool call fails input
+   * validation; can either programmatically fix the call or do a focused
+   * sub-call to the model to repair it. Returning a corrected toolCall
+   * skips the tool-error path entirely.
+   */
+  repairToolCall?: Parameters<
+    typeof streamText
+  >[0]["experimental_repairToolCall"];
   /**
    * Optional structured-output specification (AI SDK `output`).
    * Forwarded to `streamText` so the model's final response is parsed
diff --git a/dist/think.js b/dist/think.js
index 3e79f48f638f263499e9ee2a0226d1b0a0cc22ed..6e2d466bdfaa153b928d1fe18abebefa74d10a3c 100644
--- a/dist/think.js
+++ b/dist/think.js
@@ -2467,6 +2467,7 @@ var Think = class Think extends Agent {
 			providerOptions: config.providerOptions,
 			experimental_telemetry: config.experimental_telemetry,
 			experimental_transform: config.experimental_transform,
+			experimental_repairToolCall: config.repairToolCall,
 			output: finalOutput,
 			abortSignal: input.signal,
 			prepareStep: (async (event) => {
