// The shell is rendered by ./layout.tsx so it persists across the
// /transactions sub-route. Page bodies are intentionally empty.
export default function LedgerAccountPage(): null {
  return null
}
